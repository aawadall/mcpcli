export function {{.Capability.Name}}(req) {
  // TODO: implement capability logic for {{.Capability.Name}}
  return {
    result: { message: 'Capability {{.Capability.Name}} enabled' },
    id: req.id
  };
}
